# 👤 Создание пользователя "Женя" - Пошагово

## Проблема
Пользователь не существует в базе данных, поэтому приложение не может загрузить профиль.

## ✅ Решение (4 простых шага)

### Шаг 1: Создать пользователя через Dashboard

1. Откройте: https://supabase.com/dashboard/project/ihivunmndpsysxdtedti/auth/users
2. Нажмите **"Add user"** → **"Create new user"**
3. Заполните:
   ```
   Email: zhenya@twodo.app
   Password: Zhenya2025!
   Auto Confirm User: ✅ (ОБЯЗАТЕЛЬНО!)
   ```
4. Нажмите **"Create user"**
5. **СКОПИРУЙТЕ UUID** пользователя из списка (длинная строка)

### Шаг 2: Создать профиль через SQL

1. Откройте: https://supabase.com/dashboard/project/ihivunmndpsysxdtedti/sql/new
2. Вставьте (замените `YOUR_USER_ID` на скопированный UUID):

```sql
-- Создать профиль
INSERT INTO public.users (id, name, initials, avatar_color)
VALUES (
  'YOUR_USER_ID', -- <-- ВСТАВЬТЕ UUID СЮДА
  'Женя',
  'Ж',
  'emerald'
);

-- Создать личное пространство
INSERT INTO public.spaces (title, icon, type, owner_id)
VALUES (
  'Мое пространство',
  '🏠',
  'personal',
  'YOUR_USER_ID' -- <-- ТОТ ЖЕ UUID
);

-- Проверка
SELECT * FROM public.users WHERE name = 'Женя';
```

3. Нажмите **Run**

### Шаг 3: Перезагрузить приложение

1. Откройте http://localhost:5173
2. Нажмите **Ctrl+Shift+R**

### Шаг 4: Войти

```
Email: zhenya@twodo.app
Password: Zhenya2025!
```

---

## ✅ Готово!

Приложение должно загрузить профиль и показать Dashboard!

---

## 🔍 Проверка

В консоли (F12) должны увидеть:
```
🔍 Loading user profile for: ваш-uuid
✅ User profile loaded: { name: 'Женя', ... }
```

---

## 🐛 Если не работает

Выполните проверку:

```sql
-- Проверить auth.users
SELECT id, email FROM auth.users WHERE email = 'zhenya@twodo.app';

-- Проверить public.users
SELECT * FROM public.users WHERE name = 'Женя';

-- Проверить spaces
SELECT * FROM public.spaces WHERE type = 'personal';
```

Все 3 запроса должны вернуть по 1 строке.
