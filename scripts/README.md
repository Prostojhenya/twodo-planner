# Скрипты для управления пользователями

## Создание пользователя "Женя"

### Подготовка

1. **Получите Service Role Key из Supabase:**
   - Откройте [Supabase Dashboard](https://supabase.com/dashboard)
   - Перейдите в Settings → API
   - Скопируйте **service_role** key (НЕ anon key!)

2. **Добавьте ключ в .env.local:**

```env
# Существующие переменные
VITE_SUPABASE_URL=https://ihivunmndpsysxdtedti.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Добавьте эту строку (замените на ваш service_role key)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imloa...
```

⚠️ **ВАЖНО**: Service Role Key имеет полные права администратора. Никогда не коммитьте его в Git!

### Установка зависимостей

```bash
npm install
```

### Запуск скрипта

```bash
npm run create-user
```

### Что делает скрипт

1. ✅ Проверяет существование пользователя с email `zhenya@twodo.app`
2. ✅ Создает пользователя в `auth.users` (если не существует)
3. ✅ Создает профиль в `public.users`
4. ✅ Создает личное пространство "Мое пространство"
5. ✅ Выводит данные для входа

### Результат

После успешного выполнения вы увидите:

```
✅ Пользователь "Женя" успешно создан!

📋 Данные для входа:
   Email: zhenya@twodo.app
   Password: Zhenya2025!
   Имя: Женя
   Инициалы: Ж
   Цвет: emerald
   Пространств: 1

🎉 Теперь можете войти в приложение!
```

### Вход в приложение

1. Запустите приложение: `npm run dev`
2. Откройте http://localhost:5173
3. Войдите с данными:
   - Email: `zhenya@twodo.app`
   - Password: `Zhenya2025!`

## Troubleshooting

### Ошибка: "Не найдены необходимые переменные окружения"

**Решение**: Убедитесь, что в `.env.local` есть:
- `VITE_SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

### Ошибка: "permission denied" или "JWT expired"

**Решение**: 
1. Проверьте, что используете **service_role** key, а не anon key
2. Убедитесь, что ключ скопирован полностью (без пробелов)
3. Попробуйте получить новый ключ из Supabase Dashboard

### Ошибка: "relation public.users does not exist"

**Решение**: Сначала выполните схему базы данных:
1. Откройте Supabase Dashboard → SQL Editor
2. Выполните содержимое файла `supabase/schema.sql`

### Ошибка: "User already exists"

**Решение**: Скрипт автоматически обновит существующего пользователя. Если нужно удалить:

```bash
# Вручную через Supabase Dashboard:
# Authentication → Users → найти zhenya@twodo.app → Delete
```

## Безопасность

⚠️ **Service Role Key**:
- Имеет полные права администратора
- Обходит все RLS policies
- Должен храниться только локально
- Никогда не коммитьте в Git
- Не используйте в клиентском коде

✅ `.env.local` уже добавлен в `.gitignore`

## Альтернативные способы

Если скрипт не работает, используйте ручной способ:
- См. `supabase/CREATE_ZHENYA_CHEATSHEET.md`
- См. `supabase/QUICK_FIX_CREATE_USER.md`
