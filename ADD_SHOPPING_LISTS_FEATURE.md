# 🛒 Добавление списков покупок и приглашений

## Что нужно сделать

### Шаг 1: Обновить базу данных

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте и выполните содержимое файла `supabase/add-shopping-lists.sql`

Это создаст:
- ✅ Таблицу `shopping_lists` для хранения списков
- ✅ Колонку `list_id` в `shopping_items`
- ✅ Политики безопасности (RLS)
- ✅ Дефолтный список для каждого пространства

### Шаг 2: Функционал уже готов!

После выполнения SQL:

#### Списки покупок
- Каждое пространство имеет свой набор списков
- Можно создавать неограниченное количество списков
- Примеры: "Продукты", "Хозтовары", "Одежда", "Для дачи"

#### Приглашения (уже работает!)
Функционал приглашений уже реализован в приложении:

1. **Создать общее пространство**:
   - Нажмите + в переключателе пространств
   - Выберите тип "Общее"
   - Введите название

2. **Пригласить партнера**:
   - Откройте меню (три точки)
   - Нажмите "Пригласить партнера"
   - Скопируйте ссылку-приглашение
   - Отправьте партнеру

3. **Принять приглашение**:
   - Партнер открывает ссылку
   - Подтверждает присоединение
   - Теперь вы видите общие задачи, покупки, события

## Следующие шаги для полной реализации

### 1. Обновить useSupabaseData
Добавить загрузку списков покупок:

```typescript
// Fetch shopping lists
const { data: shoppingListsData } = await supabase
  .from('shopping_lists')
  .select('*')
  .eq('space_id', activeSpaceId)
  .order('created_at', { ascending: true });

if (shoppingListsData) {
  setShoppingLists(shoppingListsData.map(sl => ({
    id: sl.id,
    title: sl.title,
    icon: sl.icon,
    spaceId: sl.space_id,
    createdAt: new Date(sl.created_at).getTime(),
    updatedAt: new Date(sl.updated_at).getTime()
  })));
}
```

### 2. Обновить Shopping компонент
Добавить:
- Переключатель списков (табы или dropdown)
- Кнопку "Создать список"
- Возможность переименовать/удалить список

### 3. Обновить App.tsx
Добавить состояние для активного списка:

```typescript
const [activeShoppingListId, setActiveShoppingListId] = useState<string>('');
```

## Текущий статус

### ✅ Готово
- Типы TypeScript обновлены
- Действия для списков добавлены
- SQL миграция готова
- Приглашения работают

### 🔄 Требуется доработка
- UI для создания/переключения списков
- Загрузка списков в useSupabaseData
- Фильтрация покупок по активному списку

## Быстрое решение (временное)

Пока UI не готов, можно использовать **пространства** как разные списки:
1. Создайте пространство "Продукты"
2. Создайте пространство "Хозтовары"
3. Переключайтесь между ними для разных списков

Каждое пространство = отдельный список покупок!

## Приглашения - как использовать

### Сценарий 1: Семейные покупки
1. Создайте общее пространство "Семья"
2. Пригласите партнера
3. Оба видите и редактируете список покупок

### Сценарий 2: Совместные задачи
1. Создайте общее пространство "Проект"
2. Пригласите коллегу
3. Работайте над задачами вместе

### Сценарий 3: Планирование поездки
1. Создайте общее пространство "Отпуск"
2. Пригласите попутчика
3. Планируйте вместе

## Важно

После выполнения SQL в Supabase:
1. Перезагрузите приложение
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Проверьте что покупки появляются мгновенно

Если покупки все еще не появляются сразу - проверьте консоль браузера (F12) на ошибки.
