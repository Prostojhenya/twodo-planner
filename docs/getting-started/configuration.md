# Конфигурация

> Детальное руководство по настройке переменных окружения и конфигурации приложения TwoDo

## Содержание

- [Переменные окружения](#переменные-окружения)
- [Получение Google Gemini API ключа](#получение-google-gemini-api-ключа)
- [Примеры конфигурации](#примеры-конфигурации)
- [Проверка конфигурации](#проверка-конфигурации)
- [Устранение проблем](#устранение-проблем)

## Переменные окружения

TwoDo использует переменные окружения для настройки интеграции с внешними сервисами. Все переменные окружения хранятся в файле `.env.local` в корне проекта.

### GEMINI_API_KEY

**Назначение**: API ключ для интеграции с Google Gemini AI

**Обязательность**: Опциональная (приложение работает без неё, но AI-функции будут недоступны)

**Использование**: Используется в компоненте Events для генерации AI-подсказок при создании событий

**Формат**: Строка, содержащая ваш API ключ от Google AI Studio

**Пример**:
```bash
GEMINI_API_KEY=AIzaSyD1234567890abcdefghijklmnopqrstuvwxyz
```

### Структура файла .env.local

Создайте файл `.env.local` в корневой директории проекта со следующим содержимым:

```bash
# Google Gemini AI API Key
# Получите ключ на https://aistudio.google.com/apikey
GEMINI_API_KEY=ваш_api_ключ_здесь
```

⚠️ **Важно**: 
- Файл `.env.local` не должен коммититься в Git (он уже добавлен в `.gitignore`)
- Никогда не публикуйте свой API ключ в открытом доступе
- Каждый разработчик должен создать свой собственный `.env.local` файл

## Получение Google Gemini API ключа

Google Gemini AI используется в TwoDo для генерации умных подсказок при создании событий. Следуйте этим шагам для получения API ключа:

### Шаг 1: Перейдите в Google AI Studio

Откройте в браузере: [https://aistudio.google.com/apikey](https://aistudio.google.com/apikey)

### Шаг 2: Войдите в аккаунт Google

Если вы ещё не вошли, войдите в свой аккаунт Google. Если у вас нет аккаунта, создайте его.

### Шаг 3: Создайте API ключ

1. На странице API Keys нажмите кнопку **"Create API Key"** (Создать API ключ)
2. Выберите существующий Google Cloud проект или создайте новый
3. Система сгенерирует новый API ключ

### Шаг 4: Скопируйте ключ

1. Нажмите на иконку копирования рядом с ключом
2. Сохраните ключ в безопасном месте

⚠️ **Важно**: API ключ показывается только один раз при создании. Если вы потеряете его, придётся создать новый.

### Шаг 5: Добавьте ключ в проект

1. Откройте файл `.env.local` в корне проекта (создайте его, если не существует)
2. Добавьте строку:
   ```bash
   GEMINI_API_KEY=ваш_скопированный_ключ
   ```
3. Сохраните файл

### Шаг 6: Перезапустите сервер разработки

Если сервер разработки уже запущен, перезапустите его для применения изменений:

```bash
# Остановите сервер (Ctrl+C)
# Запустите снова
npm run dev
```

### Ограничения бесплатного плана

Google Gemini API предоставляет бесплатный план с ограничениями:

- **Запросов в минуту**: 15 запросов
- **Запросов в день**: 1,500 запросов
- **Токенов в минуту**: 1 миллион токенов

Для большинства пользователей TwoDo этих лимитов более чем достаточно.

💡 **Совет**: Если вы превысили лимиты, подождите несколько минут перед следующим запросом или рассмотрите возможность перехода на платный план.

## Примеры конфигурации

### Минимальная конфигурация (без AI)

Если вы не планируете использовать AI-функции, можете не создавать `.env.local` файл или оставить его пустым:

```bash
# Файл может быть пустым или отсутствовать
```

Приложение будет работать полностью, но кнопка AI-подсказок в Events будет показывать сообщение о том, что API ключ не настроен.

### Полная конфигурация (с AI)

Для полного функционала создайте `.env.local` с API ключом:

```bash
# Google Gemini AI Configuration
GEMINI_API_KEY=AIzaSyD1234567890abcdefghijklmnopqrstuvwxyz
```

### Конфигурация для разработки

Для разработки рекомендуется использовать отдельный API ключ:

```bash
# Development API Key
GEMINI_API_KEY=AIzaSyDev_1234567890_development_key
```

### Конфигурация для продакшена

При развертывании в продакшен, настройте переменные окружения через интерфейс вашей платформы хостинга:

**Vercel**:
1. Перейдите в Settings → Environment Variables
2. Добавьте `GEMINI_API_KEY` со значением вашего ключа
3. Выберите окружения: Production, Preview, Development

**Netlify**:
1. Перейдите в Site settings → Environment variables
2. Добавьте `GEMINI_API_KEY` со значением вашего ключа

**GitHub Pages**:
GitHub Pages не поддерживает серверные переменные окружения. Для использования AI-функций рассмотрите другие платформы хостинга.

## Проверка конфигурации

### Проверка наличия файла

Убедитесь, что файл `.env.local` существует в корне проекта:

```bash
# Linux/Mac
ls -la .env.local

# Windows (PowerShell)
dir .env.local
```

### Проверка содержимого

Откройте файл и убедитесь, что ключ установлен:

```bash
# Linux/Mac
cat .env.local

# Windows (PowerShell)
type .env.local
```

Вы должны увидеть:
```bash
GEMINI_API_KEY=ваш_ключ_здесь
```

### Проверка в приложении

1. Запустите приложение: `npm run dev`
2. Перейдите в раздел **Events** (События)
3. Нажмите кнопку "+" для создания нового события
4. Введите название события
5. Нажмите кнопку с иконкой ✨ (Sparkles) для AI-подсказки

**Если конфигурация правильная**: Вы увидите AI-сгенерированные подсказки на русском языке

**Если конфигурация неправильная**: Вы увидите сообщение "API Key not configured" или "Не удалось получить совет от AI"

### Проверка в консоли браузера

Откройте DevTools (F12) и проверьте консоль на наличие ошибок:

```javascript
// Не должно быть ошибок типа:
// "API Key not configured"
// "Failed to fetch"
// "401 Unauthorized"
```

## Устранение проблем

### Проблема: "API Key not configured"

**Причина**: Файл `.env.local` не существует или переменная не установлена

**Решение**:
1. Создайте файл `.env.local` в корне проекта
2. Добавьте строку `GEMINI_API_KEY=ваш_ключ`
3. Перезапустите сервер разработки

### Проблема: "Не удалось получить совет от AI"

**Возможные причины**:
1. Неверный API ключ
2. Превышен лимит запросов
3. Проблемы с сетью

**Решение**:
1. Проверьте правильность API ключа в `.env.local`
2. Убедитесь, что ключ не содержит лишних пробелов
3. Проверьте лимиты в [Google AI Studio](https://aistudio.google.com/)
4. Проверьте подключение к интернету

### Проблема: Изменения в .env.local не применяются

**Причина**: Vite кэширует переменные окружения при запуске

**Решение**:
1. Остановите сервер разработки (Ctrl+C)
2. Запустите снова: `npm run dev`
3. Очистите кэш браузера (Ctrl+Shift+R)

### Проблема: API ключ не работает в продакшене

**Причина**: Переменные окружения не настроены на платформе хостинга

**Решение**:
1. Перейдите в настройки вашей платформы хостинга
2. Добавьте переменную окружения `GEMINI_API_KEY`
3. Пересоберите и разверните приложение

### Проблема: 401 Unauthorized

**Причина**: API ключ недействителен или отозван

**Решение**:
1. Перейдите в [Google AI Studio](https://aistudio.google.com/apikey)
2. Проверьте статус вашего ключа
3. Создайте новый ключ при необходимости
4. Обновите `.env.local` с новым ключом

## См. также

- [Установка](./installation.md) - Полное руководство по установке проекта
- [Быстрый старт](./quick-start.md) - Минимальные шаги для запуска
- [События](../user-guide/events.md) - Как использовать AI-подсказки в Events
- [Развертывание](../deployment/production.md) - Настройка переменных окружения в продакшене
- [FAQ](../troubleshooting/faq.md) - Часто задаваемые вопросы

---

[← Назад: Быстрый старт](./quick-start.md) | [Далее: Обзор приложения →](../user-guide/overview.md)
