# Установка

> Пошаговое руководство по установке и настройке проекта TwoDo на вашем локальном компьютере.

## Содержание

- [Системные требования](#системные-требования)
- [Шаги установки](#шаги-установки)
  - [1. Клонирование репозитория](#1-клонирование-репозитория)
  - [2. Установка зависимостей](#2-установка-зависимостей)
  - [3. Настройка переменных окружения](#3-настройка-переменных-окружения)
  - [4. Запуск приложения](#4-запуск-приложения)
- [Проверка установки](#проверка-установки)
- [Возможные проблемы](#возможные-проблемы)
- [Следующие шаги](#следующие-шаги)

## Системные требования

Перед началом установки убедитесь, что на вашем компьютере установлены следующие компоненты:

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 (устанавливается вместе с Node.js)

### Проверка версий

Вы можете проверить установленные версии с помощью следующих команд:

```bash
node --version
npm --version
```

Если Node.js не установлен или версия ниже требуемой, скачайте и установите последнюю LTS версию с [официального сайта Node.js](https://nodejs.org/).

## Шаги установки

### 1. Клонирование репозитория

Клонируйте репозиторий проекта TwoDo на ваш локальный компьютер:

```bash
git clone https://github.com/your-username/twodo.git
```

Перейдите в директорию проекта:

```bash
cd twodo
```

> 📝 **Примечание**: Замените `your-username` на фактическое имя пользователя или организации GitHub, где размещен репозиторий.

### 2. Установка зависимостей

Установите все необходимые зависимости проекта с помощью npm:

```bash
npm install
```

Эта команда установит все пакеты, указанные в `package.json`, включая:

- **React 19** - библиотека для создания пользовательских интерфейсов
- **React Router DOM** - маршрутизация для React приложений
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый инструмент сборки
- **Recharts** - библиотека для визуализации данных
- **Lucide React** - набор иконок
- **Google Generative AI** - интеграция с Google Gemini API

Процесс установки может занять несколько минут в зависимости от скорости вашего интернет-соединения.

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корневой директории проекта для хранения переменных окружения:

```bash
touch .env.local
```

Откройте файл `.env.local` в текстовом редакторе и добавьте следующую переменную:

```env
GEMINI_API_KEY=your_actual_api_key_here
```

#### Получение Google Gemini API ключа

Для полной функциональности приложения вам потребуется API ключ Google Gemini:

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите в свой аккаунт Google
3. Нажмите "Create API Key" (Создать API ключ)
4. Скопируйте сгенерированный ключ
5. Вставьте его в файл `.env.local` вместо `your_actual_api_key_here`

> ⚠️ **Важно**: Никогда не публикуйте файл `.env.local` в публичных репозиториях. Этот файл уже добавлен в `.gitignore` для вашей безопасности.

> 💡 **Совет**: Если вы не планируете использовать функции AI, вы можете оставить значение по умолчанию `PLACEHOLDER_API_KEY`, но некоторые функции могут быть недоступны.

### 4. Запуск приложения

Запустите приложение в режиме разработки:

```bash
npm run dev
```

После успешного запуска вы увидите сообщение в терминале:

```
VITE v6.2.0  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
➜  press h + enter to show help
```

Откройте браузер и перейдите по адресу [http://localhost:5173/](http://localhost:5173/)

🎉 **Поздравляем!** Приложение TwoDo запущено и готово к использованию.

## Проверка установки

Чтобы убедиться, что установка прошла успешно:

1. ✅ Приложение открывается в браузере без ошибок
2. ✅ Вы видите экран приветствия TwoDo
3. ✅ В консоли браузера (F12) нет критических ошибок
4. ✅ В терминале нет сообщений об ошибках

## Возможные проблемы

### Ошибка: "command not found: node"

**Решение**: Node.js не установлен. Установите Node.js с [официального сайта](https://nodejs.org/).

### Ошибка при установке зависимостей

**Решение**: 
```bash
# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Очистите кэш npm
npm cache clean --force

# Установите зависимости заново
npm install
```

### Порт 5173 уже занят

**Решение**: Vite автоматически выберет другой порт (5174, 5175 и т.д.). Используйте адрес, указанный в терминале.

Или вы можете явно указать другой порт:

```bash
npm run dev -- --port 3000
```

### Проблемы с TypeScript

**Решение**: Убедитесь, что используете Node.js >= 18.0.0:

```bash
node --version
```

Если версия ниже, обновите Node.js.

## Следующие шаги

После успешной установки:

- 📖 Прочитайте [Быстрый старт](./quick-start.md) для знакомства с основными функциями
- 🔧 Изучите [Конфигурацию](./configuration.md) для детальной настройки
- 👥 Ознакомьтесь с [Руководством пользователя](../user-guide/overview.md) для полного понимания возможностей приложения

---

**См. также:**
- [Быстрый старт](./quick-start.md)
- [Конфигурация](./configuration.md)
- [FAQ и устранение неполадок](../troubleshooting/faq.md)
