# Инструкция по созданию пользователя "Женя"

## ⚠️ Если получаете ошибку 500

Если видите ошибку `POST .../auth/v1/token?grant_type=password 500`, используйте **Способ 1** ниже (через Dashboard).

## ✅ Способ 1: Через Supabase Dashboard (РЕКОМЕНДУЕТСЯ и НАДЕЖНО)

### Шаг 1: Создайте пользователя через UI

1. Перейдите в [Supabase Dashboard](https://supabase.com/dashboard)
2. Откройте ваш проект
3. В левом меню выберите **Authentication** → **Users**
4. Нажмите **Add user** → **Create new user**
5. Заполните форму:
   - **Email**: `zhenya@twodo.app`
   - **Password**: `Zhenya2025!`
   - **Auto Confirm User**: ✅ (ОБЯЗАТЕЛЬНО включить!)
6. Нажмите **Create user**
7. **ВАЖНО**: Скопируйте UUID пользователя из списка (выглядит как `a1b2c3d4-e5f6-...`)

### Шаг 2: Добавьте профиль через SQL

1. В левом меню выберите **SQL Editor**
2. Нажмите **New Query**
3. Вставьте этот код (замените `YOUR_USER_UUID` на скопированный UUID):

```sql
DO $$
DECLARE
  user_uuid UUID := 'YOUR_USER_UUID'; -- <-- ВСТАВЬТЕ UUID СЮДА
  new_space_id UUID;
BEGIN
  -- Создаем профиль
  INSERT INTO public.users (id, name, initials, avatar_color)
  VALUES (user_uuid, 'Женя', 'Ж', 'emerald');
  
  -- Создаем личное пространство
  new_space_id := gen_random_uuid();
  INSERT INTO public.spaces (title, icon, type, owner_id)
  VALUES ('Мое пространство', '🏠', 'personal', user_uuid);
  
  RAISE NOTICE '✅ Готово! Space ID: %', new_space_id;
END $$;
```

4. Нажмите **Run**

### Шаг 3: Войдите в приложение

Теперь можете войти:
- **Email**: `zhenya@twodo.app`
- **Password**: `Zhenya2025!`

## Данные созданного пользователя

- **Имя**: Женя
- **Инициалы**: Ж
- **Цвет аватара**: Emerald (зеленый)
- **Email**: zhenya@twodo.app
- **Пароль**: Zhenya2025!
- **Личное пространство**: "Мое пространство" 🏠

## Изменение данных

Если хотите изменить email или пароль, отредактируйте файл `create-user-zhenya-simple.sql`:

```sql
-- Измените эту строку для другого email:
email,
'zhenya@twodo.app',  -- <-- Здесь

-- Измените эту строку для другого пароля:
encrypted_password,
crypt('Zhenya2025!', gen_salt('bf')),  -- <-- Здесь
```

## ✅ Способ 2: Через SQL (если у вас есть права администратора)

Используйте файл `supabase/create-user-zhenya-safe.sql` или `supabase/reset-and-create-zhenya.sql`.

**Внимание**: Этот способ может не работать из-за ограничений прав доступа. Используйте Способ 1.

## Проверка

Чтобы убедиться, что пользователь создан правильно:

```sql
-- Проверить пользователя
SELECT * FROM public.users WHERE name = 'Женя';

-- Проверить пространства пользователя
SELECT s.* 
FROM public.spaces s
JOIN public.users u ON s.owner_id = u.id
WHERE u.name = 'Женя';
```

## Удаление пользователя (если нужно)

Если нужно удалить пользователя и начать заново:

```sql
-- Удалить пользователя (каскадно удалит все связанные данные)
DELETE FROM auth.users WHERE email = 'zhenya@twodo.app';
```

## Troubleshooting

### Ошибка: "permission denied for table auth.users"

Выполните скрипт от имени администратора или используйте альтернативный способ через Supabase Auth UI.

### Ошибка: "duplicate key value violates unique constraint"

Пользователь с таким email уже существует. Либо используйте другой email, либо удалите существующего пользователя.

### Пользователь создан, но не может войти

Убедитесь, что:
1. Email provider включен в Authentication → Providers
2. Email подтвержден (email_confirmed_at не NULL)
3. Пароль правильный

## Безопасность

⚠️ **Важно**: 
- Измените пароль после первого входа
- Не используйте этот скрипт в продакшене
- В продакшене пользователи должны регистрироваться через форму регистрации
